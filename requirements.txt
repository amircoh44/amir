# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0           # signed sessions / CSRF tokens

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0               # async PostgreSQL driver
psycopg2-binary==2.9.10       # sync driver for Alembic / scripts
alembic==1.14.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Auth / security ---
passlib[argon2]==1.7.4        # password hashing (argon2)
argon2-cffi==23.1.0
pyjwt==2.10.1
authlib==1.4.0                # Google OAuth (OpenID Connect)
httpx==0.28.1

# --- Payments ---
stripe==11.4.1

# --- Email ---
sendgrid==6.11.0

# --- Rate limiting / caching ---
slowapi==0.1.9
redis==5.2.1

# --- Utilities ---
python-slugify==8.0.4
tenacity==9.0.0
